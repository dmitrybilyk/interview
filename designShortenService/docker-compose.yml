version: "3.8"

services:
  zookeeper:
    image: zookeeper:3.7.2
    container_name: zookeeper
    environment:
      ZOO_MY_ID: 1
      ZOO_4LW_COMMANDS_WHITELIST: "*"
    ports:
      - "2181:2181"

  postgres:
    image: postgres:15-alpine
    container_name: postgres
    environment:
      POSTGRES_DB: shortdb
      POSTGRES_USER: shortuser
      POSTGRES_PASSWORD: shortpass
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U shortuser -d shortdb" ]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    container_name: redis
    command: redis-server --save 60 1 --loglevel warning
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
#
#  keycloak:
#    image: quay.io/keycloak/keycloak:21.1
#    environment:
#      KEYCLOAK_ADMIN: admin
#      KEYCLOAK_ADMIN_PASSWORD: admin
#    command: [ "start-dev", "--import-realm" ]
#    volumes:
#      - ./keycloak-realm.json:/opt/keycloak/data/import/realm-export.json:ro
#    ports:
#      - "8081:8080"

#  prometheus:
#    image: prom/prometheus:latest
#    container_name: prometheus
#    volumes:
#      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
#      - prometheus-data:/prometheus
#    command:
#      - "--config.file=/etc/prometheus/prometheus.yml"
#      - "--storage.tsdb.path=/prometheus"
#    ports:
#      - "9090:9090"
#    restart: unless-stopped
#
#  grafana:
#    image: grafana/grafana:latest
#    container_name: grafana
#    environment:
#      GF_SECURITY_ADMIN_PASSWORD: admin
#      GF_USERS_ALLOW_SIGN_UP: false
#    volumes:
#      - grafana-data:/var/lib/grafana
#    ports:
#      - "3000:3000"
#    depends_on:
#      - prometheus
#    restart: unless-stopped

  nginx:
    image: nginx:stable
    container_name: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - generator1
      - redirector1
      - redirector2
#      - generator2
#      - generator3
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost/health" ]
      interval: 10s
      timeout: 5s
      retries: 3

#  gateway:
#    build:
#      context: ../designUrlShortenerApiGateway
#      dockerfile: Dockerfile
#    container_name: gateway
#    environment:
#      SERVER_PORT: 8080
#      SPRING_REDIS_HOST: redis
#      SPRING_REDIS_PORT: 6379
#    depends_on:
#      redis:
#        condition: service_healthy
#      generator1:
#        condition: service_healthy
#    ports:
#      - "8080:8080"
#    expose:
#      - "8080"
#    healthcheck:
#      test: [ "CMD", "curl", "-f", "http://localhost:8080/actuator/health/liveness" ]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#      start_period: 30s

  generator1:
    build:
      context: ../designUrlShortenerGenerator
      dockerfile: Dockerfile
    container_name: generator1
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/shortdb
      SPRING_DATASOURCE_USERNAME: shortuser
      SPRING_DATASOURCE_PASSWORD: shortpass
      ZK_CONNECT: zookeeper:2181
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://keycloak:8080/realms/url-shortener
      SERVER_PORT: 8082
      SERVER_ADDRESS: "0.0.0.0"
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info
    depends_on:
      - postgres
      - zookeeper
    expose:
      - "8082"
    ports:
      - "8082:8082"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8082/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 20s

#  generator2:
#    build:
#      context: ../designUrlShortenerGenerator
#      dockerfile: Dockerfile
#    container_name: generator2
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/shortdb
#      SPRING_DATASOURCE_USERNAME: shortuser
#      SPRING_DATASOURCE_PASSWORD: shortpass
#      ZK_CONNECT: zookeeper:2181
#      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://keycloak:8080/realms/url-shortener
#      SERVER_PORT: 8083
#      SERVER_ADDRESS: "0.0.0.0"
#      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info
#    depends_on:
#      - postgres
#      - zookeeper
#    expose:
#      - "8083"
#    ports:
#      - "8083:8083"
#    healthcheck:
#      test: [ "CMD", "curl", "-f", "http://localhost:8083/actuator/health" ]
#      interval: 10s
#      timeout: 5s
#      retries: 12
#      start_period: 30s

#  generator3:
#    build:
#      context: ../designUrlShortenerGenerator
#      dockerfile: Dockerfile
#    container_name: generator3
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/shortdb
#      SPRING_DATASOURCE_USERNAME: shortuser
#      SPRING_DATASOURCE_PASSWORD: shortpass
#      ZK_CONNECT: zookeeper:2181
#      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: http://keycloak:8080/realms/url-shortener
#      SERVER_PORT: 8084
#      SERVER_ADDRESS: "0.0.0.0"
#      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info
#    depends_on:
#      - postgres
#      - zookeeper
#    expose:
#      - "8084"
#    ports:
#      - "8084:8084"
#    healthcheck:
#      test: [ "CMD", "curl", "-f", "http://localhost:8084/actuator/health" ]
#      interval: 10s
#      timeout: 5s
#      retries: 8
#      start_period: 20s

  redirector1:
    build:
      context: ../designUrlShortenerRedirector   # <-- new module
      dockerfile: Dockerfile
    container_name: redirector1
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/shortdb
      SPRING_DATASOURCE_USERNAME: shortuser
      SPRING_DATASOURCE_PASSWORD: shortpass
      SERVER_PORT: 8085
      SERVER_ADDRESS: "0.0.0.0"
      SPRING_REDIS_HOST: redis
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,prometheus
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8085:8085"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8085/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 30s

  redirector2:
    build:
      context: ../designUrlShortenerRedirector   # <-- new module
      dockerfile: Dockerfile
    container_name: redirector2
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/shortdb
      SPRING_DATASOURCE_USERNAME: shortuser
      SPRING_DATASOURCE_PASSWORD: shortpass
      SERVER_PORT: 8086
      SERVER_ADDRESS: "0.0.0.0"
      SPRING_REDIS_HOST: redis
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,prometheus
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8086:8086"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8086/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 30s

volumes:
  pgdata:
  redis-data:
#  prometheus-data:
#  grafana-data:
