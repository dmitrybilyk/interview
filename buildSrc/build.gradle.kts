plugins {
    `kotlin-dsl` // Provides Kotlin DSL support for build scripts
}

repositories {
    mavenCentral()
}

dependencies {
    implementation("org.jetbrains.kotlin:kotlin-stdlib:1.9.10") // Use the appropriate Kotlin version
}