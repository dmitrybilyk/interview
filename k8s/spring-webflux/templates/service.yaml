apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-webflux.fullname" . }}
  labels:
    {{- include "spring-webflux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-webflux.selectorLabels" . | nindent 4 }}
