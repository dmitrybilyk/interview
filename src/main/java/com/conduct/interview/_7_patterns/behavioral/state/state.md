State pattern allows to object to change it's
behavior without any changes. The internal state 
could be changed.

Context has State. State has Context. Implementations
of the State can switch states.