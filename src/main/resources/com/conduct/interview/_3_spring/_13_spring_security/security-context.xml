<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       https://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <bean id="userAuthorities" class="org.springframework.security.core.authority.AuthorityUtils"
          factory-method="createAuthorityList">
        <constructor-arg value="ROLE_USER"/>
    </bean>

    <!-- calling an instance method as a "factory method" - this is how you can run
         arbitrary logic (here: BCrypt-hashing a raw password) while building the context -->
    <bean id="alicePasswordHash" factory-bean="passwordEncoder" factory-method="encode">
        <constructor-arg value="secret123"/>
    </bean>

    <bean id="aliceUserDetails" class="org.springframework.security.core.userdetails.User">
        <constructor-arg value="alice"/>
        <constructor-arg ref="alicePasswordHash"/>
        <constructor-arg ref="userAuthorities"/>
    </bean>

    <!-- the simplest UserDetailsService: a plain in-memory map -->
    <bean id="userDetailsService" class="org.springframework.security.provisioning.InMemoryUserDetailsManager">
        <constructor-arg>
            <list>
                <ref bean="aliceUserDetails"/>
            </list>
        </constructor-arg>
    </bean>

    <bean id="authenticationProvider" class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
        <property name="userDetailsService" ref="userDetailsService"/>
        <property name="passwordEncoder" ref="passwordEncoder"/>
    </bean>

    <!-- the standard AuthenticationManager: tries each configured provider in turn -->
    <bean id="authenticationManager" class="org.springframework.security.authentication.ProviderManager">
        <constructor-arg>
            <list>
                <ref bean="authenticationProvider"/>
            </list>
        </constructor-arg>
    </bean>

</beans>
